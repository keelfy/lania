import ClaimsMap from './claims-map'

// Prototype: checks that chunk coordinates drawn over squaremap tiles match the game.
export default function ClaimsPage() {
  return (
    <div className="py-6">
      <ClaimsMap />
    </div>
  )
}
